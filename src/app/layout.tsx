import React from 'react'

// Root layout minimal - chaque route group gère son propre html/body
export default function RootLayout({ children }: { children: React.ReactNode }) {
  return children
}
